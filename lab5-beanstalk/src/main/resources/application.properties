spring.application.name=lab5-beanstalk

# Beanstalk's nginx proxies 80 -> 5000. Tomcat must listen there.
server.port=5000

# Expose health endpoint with details
management.endpoint.health.show-details=always